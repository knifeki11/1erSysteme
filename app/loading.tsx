import { LoadingLogoVideo } from "@/components/loading-logo-video"

/**
 * Shown by Next.js during route transitions (e.g. navigating to /solutions, /contact)
 * and while the initial page is loading.
 */
export default function Loading() {
  return <LoadingLogoVideo />
}
